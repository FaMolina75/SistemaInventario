<Window x:Class="InvemtarioHadware.Vistas.HistorialMantenimiento"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de Mantenimiento" Height="550" Width="900" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#FF5722" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="Registrar Mantenimiento" FontWeight="Bold" FontSize="16" Foreground="#FF5722" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <Label Content="Seleccionar Equipo:"/>
                <ComboBox x:Name="cbHardware" Height="25" Margin="0,0,0,10" DisplayMemberPath="SerieModelo" SelectedValuePath="idHardware"/>

                <Label Content="Proveedor de Servicio:"/>
                <DockPanel Margin="0,0,0,10">
                    <Button Content="+" Width="25" DockPanel.Dock="Right" Background="#DDDDDD" Click="btnNuevoProveedor_Click"/>
                    <ComboBox x:Name="cbProveedor" Height="25" DisplayMemberPath="NombreProveedor" SelectedValuePath="idProveedor"/>
                </DockPanel>

                <Label Content="Fecha de Ingreso:"/>
                <DatePicker x:Name="dpFecha" Height="25" Margin="0,0,0,10"/>

                <Label Content="Costo ($):"/>
                <TextBox x:Name="txtCosto" Height="25" Margin="0,0,0,10"/>

                <Label Content="Descripción del Problema:"/>
                <TextBox x:Name="txtDescripcion" Height="60" TextWrapping="Wrap" Margin="0,0,0,20"/>

                <Button x:Name="btnRegistrar" Content="ENVIAR A MANTENIMIENTO" Height="35" Background="#FF5722" Foreground="White" FontWeight="Bold" Click="btnRegistrar_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Bitácora de Reparaciones" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

            <DataGrid x:Name="dgMantenimientos" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=d}" Width="90"/>
                    <DataGridTextColumn Header="Equipo" Binding="{Binding Serie}" Width="80"/>
                    <DataGridTextColumn Header="Proveedor" Binding="{Binding NombreProveedor}" Width="120"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="Costo" Binding="{Binding Costo, StringFormat=c}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>